# URF Paper

Canonical LaTeX source: `URF_Paper_Skeleton.tex`

Build with:
```bash
latexmk -pdf URF_Paper_Skeleton.tex
```

Or use the GitHub Action under `.github/workflows/latex.yml`.

**Note:** Full `.tex` source is maintained in the Grok project artifacts and should be synced on each theory lock. If `URF_Paper_Skeleton.tex` is missing in this folder after clone, ask the project chat to re-push from `/home/workdir/artifacts/URF_Paper_Skeleton.tex`.
