# URF Sims

| File | Description |
|------|-------------|
| `URF_The_Instrument.html` | Live equation instrument (+ `instrument.css` / `instrument.js`) |
| `URF_raytrace_shortfall (7).html` | Full single-file Canvas ray-trace (packed sets, κ sweep, modes) |

Open in a modern browser. No server required.

**Pages deploy** copies the full ray-trace to `URF_raytrace_shortfall.html` for a clean URL.

**Background:** packed sets (not empty void).  
**Self-nuclearisation:** compression → explosive; expansion → sympathetic.
