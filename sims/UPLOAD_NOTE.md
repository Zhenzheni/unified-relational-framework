# Large sim files

These interactive sims are maintained in the Grok project artifacts and should be uploaded here when convenient:

| Local artifact | Repo path |
|----------------|-----------|
| `URF_The_Instrument.html` (~22 KB) | `sims/URF_The_Instrument.html` |
| `URF_raytrace_shortfall.html` (~48 KB) | `sims/URF_raytrace_shortfall.html` |
| `URF_packed_sets_2d.png` (~237 KB) | `figures/URF_packed_sets_2d.png` |

**GitHub web UI:** Add file → Upload files into `sims/` or `figures/`.

**From Grok project chat:** Ask to push those three paths to `Zhenzheni/unified-relational-framework`.

The paper LaTeX source is fully in `paper/` (master + `sections/00`–`15`).
